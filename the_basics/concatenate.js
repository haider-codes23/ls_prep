// created a variable firstName of type const and assigned it a string value
const firstName = 'Haider';
// created a variable lastName of type const and assigned it a string value
const lastName = 'Ali';
// created a variable fullname of type const which joins two strings by using the '+' operator 
const fullName = firstName + ' ' + lastName;
// finally logged the value of fullName to the console 
console.log(fullName);
